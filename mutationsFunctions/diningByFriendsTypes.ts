export type Person = {
    userId: string;
    name: string;
    location: string;
}

export type Restaurant = {
  name: string;
  location: string;
  cuisine: string;
}

export type Friend = {
  userId: string;
  friendId: string;
}